version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: protected-link
    ports:
      - "${PORT:-8080}:8080"
    depends_on:
      - radis
    env_file:
      - .env
    volumes:
      - .:/app/src
    restart: unless-stopped

  radis:
    image: redis:latest
    container_name: redis
    ports:
      - "6379:6379"
    environment:
      - REDIS_PASSWORD=${DB_PASSWORD}
    restart: unless-stopped
